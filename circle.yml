machine:
  services:
    - docker
  java:
    version: oraclejdk8

dependencies:
  override:
    # Stop preconfigured services
    - /etc/init.d/redis-server stop
    - sudo stop mongodb
    # Run docker infrastructure
    - sudo pip install --upgrade docker-compose==1.8.2
    - docker-compose up -d