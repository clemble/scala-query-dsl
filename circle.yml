machine:
  services:
    - docker
  java:
    version: oraclejdk8

dependencies:
  override:
    - docker-compose up -d

deployment:
  prod:
    branch: master
    commands:
      - sbt clean coverage test coverageReport
      - sbt coverageAggregate
      - sbt coveralls